[project]
name = "tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
 "matplotlib>=3.11.0",
 "mujoco>=3.10.0",
]

[dependency-groups]
dev = ["pybind11-stubgen>=2.5.5"]

[tool.ty.environment]
extra-paths = ["./typings"]
